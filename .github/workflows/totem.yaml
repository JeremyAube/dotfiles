on:
  push:
    branches:
      - master
    paths:
      - "keyboard/totem/**"
      - ".github/workflows/totem.yaml"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config_path: keyboard/totem/config
      build_matrix_path: keyboard/totem/build.yaml
